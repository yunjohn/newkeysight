# 迁移状态

最后更新：2026-07-31

## Signal Precision UI 重设计

- 已按 `ui/` 中的设计规范统一应用主题、字体层级、色彩和控件样式。
- 已按 `ui/UI-GUIDE.md` 重新统一工业软件布局：中文使用微软雅黑，默认字号 13，
  分组标题 15，页面标题 18，显示标题 24；普通控件高 32，主操作高 40，表格
  表头/行高 32/30；页面边距为左右 24、上下 20，窗口最低 1280×720。
- 所有新版页面间距均按 4 px 基准网格整理，并新增 UI 规范自动回归测试。
- 主界面已改为仪器控制台布局，并加入当前波形的降采样预览。
- 独立波形窗口已改为全屏波形视口与右侧信号控制台；保留游标、测量、偏移、书签、标注、参考波形、相位诊断和截图功能。
- 启动/刹车窗口已改为验证工作台布局；保留方案、批量、基准比较、ABZ 抖动、报告和归档功能。
- 历史信息已整合到主界面历史页和启动/刹车测试历史页。
- 已加入全量 XAML 主题资源引用审计，子窗口缺失资源会在测试阶段直接失败。
- Release 构建 0 警告、0 错误，自动化测试 60/60 通过。

## 本机验证结果

- .NET SDK：10.0.302
- `dotnet build KeysightScopeApp.slnx -c Release`：成功，0 警告，0 错误
- `dotnet test KeysightScopeApp.slnx -c Release`：60/60 通过
- Python 并行版：127 通过、2 跳过（现有 Qt 弃用警告 276 条）
- `dotnet format KeysightScopeApp.slnx --verify-no-changes --no-restore`：通过
- 推荐级 .NET 静态分析：0 警告、0 错误
- NuGet 已知漏洞扫描（含传递依赖）：未发现漏洞
- 覆盖率：Core 行 71.2% / 分支 64.6%；Infrastructure 测试运行聚合行 47.6% /
  分支 33.8%；
  App 以 WPF 绑定和启动冒烟为主，当前行覆盖率较低
- 真实仓库波形包：CH1、CH2、CH3 各 20,000 点，Python/C# 基线一致
- 用户性能波形包：CH1、CH2、CH4 各 20,000 点（共 60,000 点）；Release 首次解析
  49.09 ms，1920 像素包络抽稀与全量统计 27.32 ms，显示数据 9,361 点，
  托管内存增量约 6.98 MB（本机单次命令行测量）
- 自包含单文件发布：成功
- 启动冒烟：进程成功启动并保持响应，验证后已关闭
- 实机资源扫描：发现 2 个 USB VISA 资源；其中
  `USB0::0x2A8D::0x1766::MY62510221::0::INSTR` 已由最终版成功连接。
- 实机身份：`KEYSIGHT TECHNOLOGIES DSO-X 3024T`，序列号 `MY62510221`。
- 当前应用持有 USB VISA 会话时，独立硬件工具无法再次打开同一资源，符合独占会话行为。
- 发布文件：`artifacts/app/KeysightScopeApp.exe`
- 主 EXE 大小：173,378,127 字节
- 主 EXE SHA-256：`DCB5D0579B7DA32ECE9DDB494D7277741897D104D137F373742FF2E688B12E6E`
- ZIP 大小：80,051,340 字节
- ZIP SHA-256：`3DA2141187AC0D61E799C3D9AFBAD8511FE21554BEFBCE580FC40AE6D25B04C7`

## 已实现

- M1：解决方案分层、中央包版本、x64/.NET 10、Nullable、WPF 宿主、依赖注入、
  用户数据目录、异常捕获和原子配置。
- M2：不可变波形模型、单通道和 V1 波形包流式读写、取消/进度、严格数据校验、
  首尾点和极值保留抽稀、ScottPlot 5 三通道显示。
- M3（核心）：独立窗口、通道显隐、活动通道偏移与自动堆叠、参考 CSV 虚线叠加、
  自动范围、ScottPlot 原生缩放/平移、原始数据插值十字读数、A/B 游标拖动和边沿吸附、
  整条/视图/游标统计、视图撤销重做、书签、标注、后台抽稀和过期任务取消。
- M3（持久化）：CSV 邻接版本化 sidecar，保存视图、游标、通道偏移、书签和标注；
  实时采集工作区保存到用户设置目录，写入采用临时文件原子替换；同时记忆独立窗口
  位置、尺寸和最大化状态。
- M3（交互补齐）：纯波形 F11 模式、时间轴/纵轴独立复位、测量冻结与折叠、A/B
  键盘快捷放置，以及最小值、最大值、上升沿、下降沿快速事件定位。
- M3（导出与缓存）：64 项线程安全 LRU 显示缓存，缓存键包含数据版本、通道对象、
  时间范围和像素宽度；PNG 可选择完整窗口、带覆盖层纯图或无覆盖层纯图，相位诊断
  可导出 CSV。
- M4（基础）：统计、RMS、逻辑高低、幅度、频率、脉宽、占空比、上升/下降时间、
  时间窗二分定位和线性插值。
- M5（可离线验收部分）：传输抽象、脚本传输、串行 VISA 传输包装、IDN 校验、
  通道/触发 SCPI、BYTE 波形前导转换、空载荷/截断校验和多通道抓取。
- M5（2026-07-31 补齐）：Keysight IO Libraries 的真实 `ResourceManager`/
  `IMessageBasedSession` 适配、资源扫描、连接/断开、主界面抓波命令和 RAW 深存储
  `STARt/STOP` 分块读取；RAW 请求超过 50 万点时实际抓波流程自动分块并汇总进度。
- M7（离线核心）：完整/仅启动/仅刹车流程模型，目标频率/周期/RPM，连续周期判定，
  启动与刹车时间、峰值、限值判定，以及电流归零、转速归零和编码器 A 相第 N 脉冲
  回溯三种刹车完成模式。
- M8（离线核心）：位置停机抖动、AB 正交 x4 解码、ABZ 踏板释放分析、数据质量、
  版本化测试方案、三态指标、批量取消、HTML/CSV 报告和递增归档。
- 高级分析 UI：通道角色、目标频率、PPR、样品/项目设置，启动刹车与 ABZ 抖动入口，
  结果表、结果定位、持久化历史、历史导出/清理、报告导出和波形归档。
- 归档闭环：递增且不覆盖的测试目录、完整波形包、版本化元数据，以及后台生成的
  1920×1080 标准 ScottPlot 波形截图、HTML 详情和 CSV 汇总；失败目录会留下可重试标记。
- 基准比较：加载基准 CSV、按重叠时间范围对齐，逐通道比较平均值、RMS、峰峰值和
  频率，输出明确容差、差异原因和 PASS/FAIL/INCONCLUSIVE。
- 横向：中文错误、数据质量、取消、后台计算、无逐点 UI 对象、单元和集成测试骨架。
- 错误体验：CSV、截图、报告和归档对权限不足、磁盘已满及普通 I/O 失败提供包含目标
  路径的中文操作建议；所有导出尽可能使用临时文件加原子替换。
- 资源释放：独立波形窗口关闭时取消渲染、解除输入事件、清空绘图和有界缓存；高级
  分析窗口关闭时取消分析与批量任务。
- 应用宿主：单实例保护、按日/10 MB 滚动文件日志、未处理异常记录、启动偏好恢复、
  主窗口位置和尺寸持久化。
- 主窗口：设备运行控制、采集类型、设备 PNG 截图、边沿触发读取/应用、后台波形概要、
  最近 200 条版本化操作历史；最近波形列表和设备抓波自动保存到用户 captures 目录。
- 仪器交互：四通道显隐下发、垂直 Scale/Offset 读取与应用、时基/采集/触发状态读取，
  单次操作会应用触发参数、自动退出 ROLL 并异步等待触发完成或超时。
- 高级分析 UI：完整/仅启动/仅刹车、频率/周期/RPM、上下容差、连续周期、三种刹车判据、
  限值、结构化失败诊断，以及可配置 ABZ 停机抖动参数。
- Python 黄金结果：三个通道全部 17 项统计由版本化 JSON 自动跨语言比较。
- 性能工具：`dotnet/tools/KeysightScopeApp.Benchmark` 可重复测量 CSV 解析、抽稀、
  全量统计和托管内存增量。
- 性能回归：自动构造 100 万点波形，要求 1920 像素抽稀与全量统计在 5 秒内完成，
  并限制显示点数，防止退化为逐点绘制。
- 易用性：主窗口提供 Ctrl+O、Ctrl+S、Ctrl+W、F5 和 Esc 快捷键，状态、进度、游标
  与测量结果增加辅助功能名称。
- 工程质量：推荐级分析器、`.editorconfig`、七个项目锁文件、三项目覆盖率采集、
  标签自动发布 GitHub Release、ZIP/哈希和第三方许可随包交付。
- 数据迁移：应用配置 schema v1/v2→v3、测试方案 v1→v2、波形工作区 v1→v2 自动升级，
  并提供界面入口和
  `KeysightScopeApp.Migrate` 命令行工具；导入 Python 配置/历史前自动备份，源文件
  保持不变，输出摘要并允许重试。本机已实跑迁移 4 个配置文件和 1 条启动历史，0 警告。
- 版本传播：应用、程序集、窗口标题、日志、测试记录和报告统一为 v2.0.0；启动时检测
  VISA 运行库并给出中文安装提示。
- 高级分析执行：启动刹车与 ABZ 抖动在后台线程计算，可取消；替换波形时取消旧任务，
  过期结果不会覆盖新波形。通道角色冲突、数据质量不足和诊断失败都会保存正式
  INCONCLUSIVE 记录。
- 测试方案：保存/加载版本化通道角色与分析参数，可关联基准 CSV；支持可取消的重复
  批量运行，并把每次结果写入持久化历史。
- 实机验收工具：新增 `KeysightScopeApp.HardwareSmoke`，支持指定 VISA 资源执行只读
  IDN、系统错误、触发状态、时基、采集类型和四通道垂直设置检查。
- 测试隔离：App 和操作历史测试统一改用临时用户目录；加载时过滤旧测试曾泄漏的
  1970 年“测试”记录，避免继续污染真实用户历史。

## 可见 UI 验收

- 发布版主窗口可启动，辅助功能树完整，进程保持响应。
- UI 自动化首次发现 `ProgressBar.Value` 对只读属性的默认双向绑定异常；已改为明确
  `Mode=OneWay` 并增加 WPF STA 回归测试。
- 修复后重新启动无异常弹窗。
- 当前 Windows.Graphics.Capture 对该 WPF 窗口返回“不支持此接口”，因此自动点击
  文件对话框、加载后拖动和视觉截图验收未能在本机工具中继续；保留为人工验收项。

## 外部验收项

以下项目不能仅靠仓库代码关闭，需要目标硬件或人工观测：

- Keysight IO Libraries Suite 在目标机器上的真实资源发现、连接和会话兼容性实测。
- 每个目标示波器型号/固件的连接、RAW 深存储分块和设备截图实测。
- 指定 PC 的 30 FPS、100 次开关窗口、100 万点和长时间内存验收。
- 启动刹车、ABZ 停机抖动的真实工况人工验收。
- 干净 Windows 机器的安装、升级、卸载和非管理员账户验收。

## 基准差异

`bundle_20260730_165329.csv` 位于被 Git 忽略的 `captures/waveforms` 运行数据目录，
实际元数据为 CH1、CH2、CH4 各 20,000 点。仓库根目录另有
`bundle_20260325_151508.csv`，其元数据为 CH1、CH2、CH3 各 20,000 点。自动化测试
使用可提交的后者，人工性能验收使用用户指定的前者。

## 本轮差距审计修正

- 修正逻辑高低电平：按中间阈值分组后计算样本平均值。
- 修正幅度定义：使用逻辑高低电平差的一半，与 Python 基准一致。
- 修正脉宽：使用完整脉冲的平均宽度，而不是中位数。
- 脉冲计数仅统计具有后续下降沿的完整脉冲。
- 新增边沿吸附、最近脉冲、最近周期和区间 RPM 统计公共 API。
- 新增通用通道边沿对齐、时间差和归一化相位比较公共 API。
