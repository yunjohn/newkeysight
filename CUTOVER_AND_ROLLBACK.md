# C# 版切换与回退方案

## 当前状态

C# 版已完成可在无仪器环境自动验证的开发、测试和发布。Python 版保持原目录、入口和
运行数据不变。正式切换前仍须完成目标 Keysight 型号/固件、真实工况和交互流畅度验收。

## 试运行

1. 保留 Python 版 `.venv`、`main.py`、`src/` 和现有 `captures/`。
2. 将 `artifacts/app` 整体复制到试运行机器，不要只复制 EXE；WPF/Skia 原生 DLL 必须同目录保留。
3. 安装与现有 Python 版相同的 Keysight IO Libraries Suite。
4. 先执行离线 CSV 回归，再执行设备连接、NORMal、MAXimum、RAW 和深存储分块抓波。
5. 对同一组波形分别运行 Python 与 C# 分析，并保存报告与截图。
6. 观察期内 C# 版写入 `%LOCALAPPDATA%\KeysightScopeApp`，Python 版继续使用原运行目录。

## 必做验收

- 目标示波器型号和固件逐一通过连接、触发、运行控制、截图与抓波。
- 用户性能波形包加载、平移、缩放、游标拖动无明显卡顿。
- 100 万点、目标最大 RAW 点数、取消和设备断开场景通过。
- 启动、刹车、A 相回溯和 ABZ 停机抖动使用真实工况人工复核。
- 重复打开/关闭波形窗口 100 次，长时间运行无持续内存增长。
- 非管理员账户、目标 DPI、多显示器以及干净 Windows 机器通过。

## 切换

只有必做验收全部签字后，才把桌面快捷方式和日常入口指向 C# 发布目录。建议至少保留
两周并行观察期；期间任何算法差异都以原始波形和两版报告定位，不能只比较截图。

## 回退

1. 关闭 C# 版。
2. 将入口恢复为 `main.py` 或现有 Python 打包程序。
3. Python 源码和运行数据从未被迁移过程删除，因此无需还原代码。
4. C# 用户配置位于 `%LOCALAPPDATA%\KeysightScopeApp`；回退不需要删除它。
5. 把失败波形、设备 ID、固件、C# 日志和复现步骤归档后再修复。

## Python 归档条件

并行观察期结束、无阻断差异、报告和配置迁移确认后，才允许给 Python 版打只读归档标签。
归档是保留而非删除；在至少一个稳定发布周期内不得移除 Python 源码或历史波形。
